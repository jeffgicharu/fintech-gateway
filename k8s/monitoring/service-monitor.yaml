apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fintech-gateway
  labels:
    app: fintech-gateway
    release: prometheus
spec:
  selector:
    matchLabels:
      app: fintech-gateway
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
  namespaceSelector:
    matchNames:
      - default
